package meldexun.renderlib.util;

/** Stub for compilation only — provided at runtime by Nothirium. */
public class Frustum {
    public boolean isAABBInFrustum(double minX, double minY, double minZ, double maxX, double maxY, double maxZ) { throw new AssertionError(); }
}
